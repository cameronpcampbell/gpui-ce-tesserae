use palette::rgb::channels::Rgba;

use crate::{Theme, ThemeAccentsConfig, ThemeSetKind};

pub fn generate_theme(config: &ThemeAccentsConfig, kind: ThemeSetKind) -> super::Theme {
    unimplemented!()
}
